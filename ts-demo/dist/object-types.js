"use strict";
var car = {
    id: 1,
    make: 'Ford',
    model: 'T',
    year: 1924,
    color: 'black',
    price: 400,
};
var propName = "ma" + "ke";
var somePropInCar = car[propName];
var doIt = function (makePropName) {
};
console.log(car.id + car.make);
var nextId;
// if (car.id) {
nextId = car.id + 1;
// }
var newCar = {
    make: 'Ford',
    model: 'T',
    year: 1924,
    color: 'black',
    price: 400,
};
